name: cdk-pull-request-actions
run-name: ${{ github.actor }} is running ${{ github.action }} workflow
on:
  pull_request:
    types: [opened]
    branches:
      - main
jobs:
  cdk-diff:
    runs-on: ubuntu-latest
    permissions:
      id-token: write
      contents: read
      pull-requests: write
    env:
      GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
    steps:
      - uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: arn:aws:iam::533267250677:role/ghac-cdk-deploy-role
          aws-region: ap-northeast-1

      - uses: actions/checkout@v4

      - name: Use Node.js 18.19.x
        uses: actions/setup-node@v4
        with:
          node-version: 18.19.x

      - name: Setup dependencies
        if: ${{ steps.node_modules_cache_id.outputs.cache-hit != 'true' }}
        run: npm ci

      - run: |
          npx cdk diff prod/* > comments
          gh pr comment -F ./comments ${{ github.event.pull_request.html_url }}